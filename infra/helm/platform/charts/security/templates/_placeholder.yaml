apiVersion: v1
kind: ConfigMap
metadata:
  name: security-placeholder
  labels:
    chart: security
data:
  note: "This is a minimal placeholder chart used during local development / CI to avoid missing-dependency errors."
